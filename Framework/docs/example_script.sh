#!/usr/bin/env bash

# This is an example script
# It can be run with `./example_script.sh`
# It shows the adjustment needed to make a script work in a NixOS environment

# The adjustment is to add the following line at the top of the script:
# #!/usr/bin/env bash

echo "Hello World"